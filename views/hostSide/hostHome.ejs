<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <title>Document</title>
</head>
<body>
  <%- include('../partials/hostHeader') %> <!-- Header -->

  <main class="container mx-auto px-4 py-6">
    <ul class="flex flex-wrap">
      <% regHouse.forEach(home => {  %>
        <li class="bg-white shadow-md rounded-lg overflow-hidden mb-4 w-3xs mx-7 h-auto border-1">
          <img src="<%= home.HouseImage %>" alt="<%= home.HouseName %>" class="w-full h-48 object-cover">
          <div class="p-4 relative">
            <h2 class="text-xl font-bold mb-2"><%= home.HouseName %></h2>
            <p class="text-gray-700 mb-2">Price per Night: $<%= home.PricePerNight %></p>
            <p class="text-gray-700 mb-2 truncate">Location: <%= home.Location %></p>
            <p class="text-gray-700">Rating: <%= home.Rating %> / 5</p>
            <button class="bg-blue-500 text-white font-semibold py-2 px-4 rounded hover:bg-blue-600 absolute right-0 bottom-0 mx-1 my-1">Book Now</button>
          </div>
        </li>
      <% })  %>
    </ul>
  </main>

  <%- include('../partials/footer') %> <!-- Footer -->
</body>
</html>